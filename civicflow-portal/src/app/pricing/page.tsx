import PricingContent from "./PricingContent";

export const metadata = {
  title: "Pricing — Unestra Cloud",
  description: "Simple, transparent per-vertical pricing for community organizations and nonprofits. Unlimited members on every plan.",
};

export default function PricingPage() {
  return <PricingContent />;
}
