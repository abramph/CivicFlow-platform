/** Customer-facing product identity. Safe to change freely — does not affect
 * the CF- license key format, env var names, or directory/domain names. */
const PRODUCT_NAME = "Unestra";
const TAGLINE = "One organization. Fully connected.";
const ATTRIBUTION = "An APH Technologies product";
/** Prior public name, for one-time transition messaging only. */
const LEGACY_NAME = "CivicFlow";

module.exports = {
  PRODUCT_NAME,
  TAGLINE,
  ATTRIBUTION,
  LEGACY_NAME,
};
